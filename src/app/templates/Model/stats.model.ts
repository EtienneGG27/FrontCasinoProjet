export class StatsModel {
  constructor(public id: number, public games_played: number, public games_won: number, public games_lost: number, public total_tokens: number, public total_bets: number) {
  }
}
